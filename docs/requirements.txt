# Docs-only dependencies for ReadTheDocs.
# Pinned to the locally tested versions (same fix as stereopipeline-quickstart):
# an unpinned RTD build resolves sphinx-book-theme 1.4 / pydata-sphinx-theme
# 0.20, which renders a header navbar whose search button duplicates the
# sidebar search field.

sphinx==9.1.0
myst-nb==1.4.0
sphinx-autoapi==3.8.0
sphinx-design==0.7.0
sphinx-copybutton==0.5.2
sphinx-book-theme==1.2.0   # hard-pins pydata-sphinx-theme==0.16.1
